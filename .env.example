# The server reads configuration from the PROJECT-ROOT .env file by default
# (../.env). You can also place a server-specific .env here if you prefer —
# values here will override the root .env.
#
# See ../.env.example for the full list of variables and explanations.
